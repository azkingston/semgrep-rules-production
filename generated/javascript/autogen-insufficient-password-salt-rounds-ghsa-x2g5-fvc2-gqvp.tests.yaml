# ═══════════════════════════════════════════════════
# AutoGen Test Cases — GHSA-x2g5-fvc2-gqvp
# Rule:      autogen-insufficient-password-salt-rounds-ghsa-x2g5-fvc2-gqvp.yaml
# Language:  javascript
# Grade:     C (50/100)
# Format:    Semgrep test format (semgrep --test compatible)
# Usage:     semgrep --test generated/javascript/
# ═══════════════════════════════════════════════════

# HOW TO READ THIS FILE:
#   Lines marked  "# ruleid: <id>"  MUST trigger the rule  (true positives)
#   Lines marked  "# ok:"           MUST NOT trigger        (false positives)
#   Lines marked  "# edge:"         are informational only

# ── TRUE POSITIVES — rule MUST fire on these ──────────────────
# Test results from accuracy run: 1/2 caught

# TP-1: exact vulnerable pattern with 5 salt rounds default
# Accuracy result: PASS ✅
const bcrypt = require('bcrypt');

export function getHash(value) {
    const salt = bcrypt.genSaltSync(parseInt(process.env.PASSWORD_SALT_HASH_ROUNDS || '5'));
    return bcrypt.hashSync(value, salt);
# ruleid: autogen-insufficient-password-salt-rounds-ghsa-x2g5-fvc2-gqvp
}

# TP-2: insufficient salt rounds with 3 as default
# Accuracy result: MISS ❌
import bcrypt from 'bcrypt';

function hashPassword(password) {
    const saltRounds = process.env.SALT_ROUNDS || 3;
    return bcrypt.hashSync(password, bcrypt.genSaltSync(saltRounds));
# ruleid: autogen-insufficient-password-salt-rounds-ghsa-x2g5-fvc2-gqvp
}

# ── FALSE POSITIVES — rule MUST NOT fire on these ─────────────
# Test results: 2/2 clean

# FP-1: secure salt rounds with 12 as default
# Accuracy result: CLEAN ✅
# ok: const bcrypt = require('bcrypt');
# ok: 
# ok: export function getHash(value) {
# ok:     const salt = bcrypt.genSaltSync(parseInt(process.env.PASSWORD_SALT_HASH_ROUNDS || '12'));
# ok:     return bcrypt.hashSync(value, salt);
# ok: }

# FP-2: hardcoded secure salt rounds of 10
# Accuracy result: CLEAN ✅
# ok: import bcrypt from 'bcrypt';
# ok: 
# ok: function hashPassword(password) {
# ok:     const saltRounds = 10;
# ok:     return bcrypt.hashSync(password, saltRounds);
# ok: }

# ── EDGE CASES — informational, review manually ────────────────

# EDGE-1: complex environment variable fallback with borderline salt rounds → SILENT
# const bcrypt = require('bcrypt');
# 
# function createHash(value) {
#     const rounds = process.env.BCRYPT_ROUNDS || process.env.DEFAULT_ROUNDS || '8';
#     return bcrypt.hashSync(value, bcrypt.genSaltSync(parseInt(rounds)));
# }

# ── ACCURACY SUMMARY ───────────────────────────────────────────
# Grade:   C 🟠
# Score:   50/100
# TP rate: 50% (1/2)
# FP rate: 0% (0/2 false alarms)
# Edges:   0/1 fired