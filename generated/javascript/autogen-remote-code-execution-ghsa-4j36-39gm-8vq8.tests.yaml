# ═══════════════════════════════════════════════════
# AutoGen Test Cases — GHSA-4j36-39gm-8vq8
# Rule:      autogen-remote-code-execution-ghsa-4j36-39gm-8vq8.yaml
# Language:  javascript
# Grade:     A (100/100)
# Format:    semgrep --test compatible
# Usage:     semgrep --test generated/javascript/
# ═══════════════════════════════════════════════════

# # ruleid: <id>  → line below MUST trigger
# # ok: <id>      → line below MUST NOT trigger

# ── TRUE POSITIVES — rule MUST fire ─────────────────────────────
# Caught: 11/11

# TP-1: Real vulnerable code from fix PR/commit (code_search:arkenfox/user.js)
# Code search reference from: arkenfox/user.js
# File: user.js
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
# (see file for full context)

# TP-2: Express app executing user script in vm with Playwright context
const express = require('express');
const app = express();
app.use(express.json());
app.post('/execute', (req, res) => {
  const userScript = req.body.script;
  const vm = require('vm');
  const context = { browser: require('playwright').chromium };
  vm.createContext(context);
  vm.runInContext(userScript, context);
  res.send('Executed');
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
});

# TP-3: Direct exec with user input from command line arguments
const { exec } = require('child_process');
const userInput = process.argv[2];
exec(userInput, (error, stdout, stderr) => {
  if (error) {
    console.error(`exec error: ${error}`);
    return;
  }
  console.log(`stdout: ${stdout}`);
  console.error(`stderr: ${stderr}`);
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
});

# TP-4: Exec with user input from Express query parameters
const { exec } = require('child_process');
const userInput = req.query.cmd;
exec(userInput, (error, stdout, stderr) => {
  if (error) {
    console.error(`exec error: ${error}`);
    return;
  }
  console.log(`stdout: ${stdout}`);
  console.error(`stderr: ${stderr}`);
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
});

# TP-5: Async/await pattern with user input executed
const { exec } = require('child_process');
async function runCommand(userInput) {
  return new Promise((resolve, reject) => {
    exec(userInput, (error, stdout, stderr) => {
      if (error) {
        reject(`exec error: ${error}`);
      }
      resolve(`stdout: ${stdout}`);
    });
  });
}
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
runCommand(req.body.command);

# TP-6 [BYPASS: indirect_flow]: [BYPASS indirect_flow] Indirect flow through function calls and variable assignment.
const { exec } = require('child_process');

function getCommand(req) {
  return req.query.cmd;
}

function executeCommand(command) {
  exec(command, (error, stdout, stderr) => {
    if (error) {
      console.error(`exec error: ${error}`);
      return;
    }
    console.log(`stdout: ${stdout}`);
    console.error(`stderr: ${stderr}`);
  });
}

const command = getCommand(request);
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
executeCommand(command);

# TP-7 [BYPASS: indirect_flow]: [BYPASS indirect_flow] Indirect flow using a poorly implemented sanitizer that does not actually sanitize.
const { exec } = require('child_process');

function sanitizeInput(input) {
  // Incorrectly implemented sanitizer
  return input.split(' ').join(' ');
}

const userInput = request.query.cmd;
const sanitizedInput = sanitizeInput(userInput);
exec(sanitizedInput, (error, stdout, stderr) => {
  if (error) {
    console.error(`exec error: ${error}`);
    return;
  }
  console.log(`stdout: ${stdout}`);
  console.error(`stderr: ${stderr}`);
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
});

# TP-8 [BYPASS: encoding_transformation]: [BYPASS encoding_transformation] Using Base64 encoding and decoding to obfuscate the command injection.
const { exec } = require('child_process');
const base64 = require('base-64');

function executeCommand(encodedCmd) {
  const decodedCmd = base64.decode(encodedCmd);
  exec(decodedCmd, (error, stdout, stderr) => {
    if (error) {
      console.error(`exec error: ${error}`);
      return;
    }
    console.log(`stdout: ${stdout}`);
    console.error(`stderr: ${stderr}`);
  });
}

const userInput = req.query.cmd; // Assume this is tainted
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
executeCommand(userInput);

# TP-9 [BYPASS: encoding_transformation]: [BYPASS encoding_transformation] Reversing the string before execution to bypass simple pattern matching.
const { exec } = require('child_process');

function executeCommand(userInput) {
  const command = userInput.split('').reverse().join('');
  exec(command, (error, stdout, stderr) => {
    if (error) {
      console.error(`exec error: ${error}`);
      return;
    }
    console.log(`stdout: ${stdout}`);
    console.error(`stderr: ${stderr}`);
  });
}

const userInput = req.query.cmd; // Assume this is tainted
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
executeCommand(userInput);

# TP-10 [BYPASS: framework_variation]: [BYPASS framework_variation] Using req.param to access user input, which might not be covered by the rule.
const { exec } = require('child_process');
const express = require('express');
const app = express();
app.get('/run', (req, res) => {
  const cmd = req.param('cmd'); // using req.param instead of req.query or req.body
  exec(cmd, (error, stdout, stderr) => {
    if (error) {
      res.send(`Error: ${stderr}`);
    } else {
      res.send(`Output: ${stdout}`);
    }
  });
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
});

# TP-11 [BYPASS: framework_variation]: [BYPASS framework_variation] Using Koa framework which uses a different way to access request parameters.
const { exec } = require('child_process');
const Koa = require('koa');
const app = new Koa();
app.use(async ctx => {
  const cmd = ctx.request.query.cmd; // Using Koa framework instead of Express
  exec(cmd, (error, stdout, stderr) => {
    if (error) {
      ctx.body = `Error: ${stderr}`;
    } else {
      ctx.body = `Output: ${stdout}`;
    }
  });
});
# ruleid: autogen-remote-code-execution-ghsa-4j36-39gm-8vq8
app.listen(3000);

# ── FALSE POSITIVES — rule MUST NOT fire ────────────────────────
# Clean: 5/5

# FP-1: Hardcoded safe command
# ok: const { exec } = require('child_process');
# ok: exec('ls -la', (error, stdout, stderr) => {
# ok:   if (error) {
# ok:     console.error(`exec error: ${error}`);
# ok:     return;
# ok:   }
# ok:   console.log(`stdout: ${stdout}`);
# ok:   console.error(`stderr: ${stderr}`);
# ok: });

# FP-2: Hardcoded safe command stored in variable
# ok: const { exec } = require('child_process');
# ok: const safeCommand = 'echo Hello World';
# ok: exec(safeCommand, (error, stdout, stderr) => {
# ok:   if (error) {
# ok:     console.error(`exec error: ${error}`);
# ok:     return;
# ok:   }
# ok:   console.log(`stdout: ${stdout}`);
# ok:   console.error(`stderr: ${stderr}`);
# ok: });

# FP-3: Sanitized user input before execution
# ok: const { exec } = require('child_process');
# ok: const userInput = req.body.command;
# ok: const sanitizedInput = sanitize(userInput);
# ok: exec(sanitizedInput, (error, stdout, stderr) => {
# ok:   if (error) {
# ok:     console.error(`exec error: ${error}`);
# ok:     return;
# ok:   }
# ok:   console.log(`stdout: ${stdout}`);
# ok:   console.error(`stderr: ${stderr}`);
# ok: });
# ok: function sanitize(input) {
# ok:   return input.replace(/[^a-zA-Z0-9 ]/g, '');
# ok: }

# FP-4: Conditional execution of a safe command
# ok: const { exec } = require('child_process');
# ok: const command = 'ls';
# ok: if (command === 'ls') {
# ok:   exec(command, (error, stdout, stderr) => {
# ok:     if (error) {
# ok:       console.error(`exec error: ${error}`);
# ok:       return;
# ok:     }
# ok:     console.log(`stdout: ${stdout}`);
# ok:     console.error(`stderr: ${stderr}`);
# ok:   });
# ok: }

# FP-5: Whitelist-based command execution
# ok: const { exec } = require('child_process');
# ok: const safeCommands = ['ls', 'pwd'];
# ok: const command = req.body.command;
# ok: if (safeCommands.includes(command)) {
# ok:   exec(command, (error, stdout, stderr) => {
# ok:     if (error) {
# ok:       console.error(`exec error: ${error}`);
# ok:       return;
# ok:     }
# ok:     console.log(`stdout: ${stdout}`);
# ok:     console.error(`stderr: ${stderr}`);
# ok:   });
# ok: }

# ── EDGE CASES — informational ──────────────────────────────────

# EDGE-1: Config-driven command execution — debatable
# const { exec } = require('child_process');
# const config = require('./config.json');
# exec(config.safeCommand, (error, stdout, stderr) => {
#   if (error) {
#     console.error(`exec error: ${error}`);
#     return;
#   }
#   console.log(`stdout: ${stdout}`);
#   console.error(`stderr: ${stderr}`);
# });

# EDGE-2: Sanitized but still risky due to incomplete sanitization
# const { exec } = require('child_process');
# const userInput = req.body.command;
# const sanitizedInput = userInput.replace(/[^a-zA-Z0-9 ]/g, '');
# exec(sanitizedInput, (error, stdout, stderr) => {
#   if (error) {
#     console.error(`exec error: ${error}`);
#     return;
#   }
#   console.log(`stdout: ${stdout}`);
#   console.error(`stderr: ${stderr}`);
# });

# ── ADVERSARIAL ATTACK RESULTS ──────────────────────────────────
# Bypasses found: 6
# indirect_flow: ❌ BYPASSED — The rule under attack focuses on direct taint flows from sources to sinks. By introducing intermediate variables and functions, the taint flow can be obscured, potentially bypassing the rule.
# encoding_transformation: ❌ BYPASSED — The rule does not account for transformations or wrappers that can be applied to user input before it reaches the sink. By using encoding and decoding, or simple string operations, the tainted data can bypass the pattern detection and still lead to a command injection vulnerability.
# framework_variation: ❌ BYPASSED — The rule focuses on specific patterns and APIs, mainly targeting direct usage of exec and similar functions. By using alternative methods or libraries that perform similar actions, such as using a different child process library or a different method of accessing user input, it is possible to bypass the rule.

# ── ACCURACY SUMMARY ────────────────────────────────────────────
# Grade:   A 🟢
# Score:   100/100
# TP rate: 100% (11/11)
# FP rate: 0% (0/5 false alarms)