# ═══════════════════════════════════════════════════
# AutoGen Test Cases — GHSA-c8m8-3jcr-6rj5
# Rule:      autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5.yaml
# Language:  javascript
# Grade:     F (0/100)
# Format:    semgrep --test compatible
# Usage:     semgrep --test generated/javascript/
# ═══════════════════════════════════════════════════

# # ruleid: <id>  → line below MUST trigger
# # ok: <id>      → line below MUST NOT trigger

# ── TRUE POSITIVES — rule MUST fire ─────────────────────────────
# Caught: 0/11

# TP-1: Real vulnerable code from fix PR/commit (code_search:gitlabhq/gitlabhq)
# Code search reference from: gitlabhq/gitlabhq
# File: .eslint_todo/gitlab-no-hardcoded-urls.mjs
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
# (see file for full context)

# TP-2: Hardcoded JWT secret
const jwt = require('jsonwebtoken');
const secret = 'frangoteam751';
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign({ user: 'admin' }, secret);

# TP-3: Express app with hardcoded secret
const express = require('express');
const app = express();
app.post('/login', (req, res) => {
  const secret = 'frangoteam751';
  const token = jwt.sign({ user: req.body.user }, secret);
  res.send(token);
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
});

# TP-4: Fallback to hardcoded secret
const secret = process.env.JWT_SECRET || 'frangoteam751';
function generateToken(user) {
  return jwt.sign({ user }, secret);
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
}

# TP-5: Async function with hardcoded fallback secret
async function getToken(user) {
  const secret = await getSecret() || 'frangoteam751';
  return jwt.sign({ user }, secret);
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
}

# TP-6 [BYPASS: indirect_flow]: [BYPASS indirect_flow] Indirect via function call returning the secret.
const jwt = require('jsonwebtoken');

function getSecret() {
    return 'frangoteam751';
}

const secret = getSecret();
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign({ user: 'admin' }, secret);

# TP-7 [BYPASS: indirect_flow]: [BYPASS indirect_flow] Indirect via arrow function returning the secret.
const jwt = require('jsonwebtoken');

const secretProvider = () => {
    const secret = 'frangoteam751';
    return secret;
};

# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign({ user: 'admin' }, secretProvider());

# TP-8 [BYPASS: encoding_transformation]: [BYPASS encoding_transformation] The secret is encoded and then decoded before being used, which bypasses the direct pattern checks.
const jwt = require('jsonwebtoken');

function encodeSecret(secret) {
  return Buffer.from(secret).toString('base64');
}

const hardcodedSecret = 'frangoteam751';
const encodedSecret = encodeSecret(hardcodedSecret);
const decodedSecret = Buffer.from(encodedSecret, 'base64').toString('utf8');

# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign({ data: 'test' }, decodedSecret);

# TP-9 [BYPASS: encoding_transformation]: [BYPASS encoding_transformation] The secret is transformed by reversing it and then reversed back before use, bypassing the direct pattern checks.
const jwt = require('jsonwebtoken');

function transformSecret(secret) {
  return secret.split('').reverse().join('');
}

const hardcodedSecret = 'frangoteam751';
const transformedSecret = transformSecret(hardcodedSecret);
const originalSecret = transformSecret(transformedSecret);

# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign({ data: 'test' }, originalSecret);

# TP-10 [BYPASS: framework_variation]: [BYPASS framework_variation] Using 'jose' library instead of 'jsonwebtoken' with hardcoded secret.
const jose = require('jose');

const secret = 'frangoteam751';
const payload = { user: 'admin' };

async function signToken() {
  const jwt = await new jose.SignJWT(payload)
    .setProtectedHeader({ alg: 'HS256' })
    .sign(new TextEncoder().encode(secret));
  console.log(jwt);
}
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
signToken();

# TP-11 [BYPASS: framework_variation]: [BYPASS framework_variation] Manual JWT creation using 'crypto' module with hardcoded secret.
const crypto = require('crypto');

const secret = 'frangoteam751';
const payload = JSON.stringify({ user: 'admin' });

function signToken() {
  const header = JSON.stringify({ alg: 'HS256', typ: 'JWT' });
  const base64Header = Buffer.from(header).toString('base64');
  const base64Payload = Buffer.from(payload).toString('base64');
  const data = base64Header + '.' + base64Payload;
  const signature = crypto
    .createHmac('sha256', secret)
    .update(data)
    .digest('base64');
  const jwt = data + '.' + signature;
  console.log(jwt);
}
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
signToken();

# ── FALSE POSITIVES — rule MUST NOT fire ────────────────────────
# Clean: 5/5

# FP-1: Environment variable for secret
# ok: const jwt = require('jsonwebtoken');
# ok: const secret = process.env.JWT_SECRET;
# ok: const token = jwt.sign({ user: 'admin' }, secret);

# FP-2: Secret from request body
# ok: const express = require('express');
# ok: const app = express();
# ok: app.post('/login', (req, res) => {
# ok:   const secret = req.body.secret;
# ok:   const token = jwt.sign({ user: req.body.user }, secret);
# ok:   res.send(token);
# ok: });

# FP-3: Dynamic secret retrieval
# ok: const secret = getDynamicSecret();
# ok: function generateToken(user) {
# ok:   return jwt.sign({ user }, secret);
# ok: }

# FP-4: Async secret from external service
# ok: async function getToken(user) {
# ok:   const secret = await fetchSecretFromService();
# ok:   return jwt.sign({ user }, secret);
# ok: }

# FP-5: Secure random secret generation
# ok: function createToken(user) {
# ok:   const secret = crypto.randomBytes(64).toString('hex');
# ok:   return jwt.sign({ user }, secret);
# ok: }
# ok: const userToken = createToken('admin');

# ── EDGE CASES — informational ──────────────────────────────────

# EDGE-1: Config-driven secret with fallback
# const secret = config.get('jwtSecret') || 'frangoteam751';
# function generateToken(user) {
#   return jwt.sign({ user }, secret);
# }

# EDGE-2: Sanitized input with fallback secret
# const userInput = sanitizeInput(req.body.secret);
# const secret = userInput || 'frangoteam751';
# const token = jwt.sign({ user: req.body.user }, secret);

# ── ADVERSARIAL ATTACK RESULTS ──────────────────────────────────
# Bypasses found: 6
# indirect_flow: ❌ BYPASSED — The rule might not catch indirect flows where the secret is assigned to a variable through a function call or intermediate variable, which is then used in the JWT signing process.
# encoding_transformation: ❌ BYPASSED — The rule primarily checks for direct use of hardcoded secrets in the jwt.sign function. By transforming the secret through encoding or other operations, we can bypass the direct pattern checks while still using a hardcoded secret.
# framework_variation: ❌ BYPASSED — The rule focuses on specific patterns related to the 'jsonwebtoken' library and hardcoded secrets. By using a different library or method to sign JWTs, the rule may not detect the vulnerability.

# ── ACCURACY SUMMARY ────────────────────────────────────────────
# Grade:   F 🔴
# Score:   0/100
# TP rate: 0% (0/11)
# FP rate: 0% (0/5 false alarms)