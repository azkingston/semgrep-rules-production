# ═══════════════════════════════════════════════════
# AutoGen Test Cases — GHSA-c8m8-3jcr-6rj5
# Rule:      autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5.yaml
# Language:  javascript
# Grade:     F (0/100)
# Format:    Semgrep test format (semgrep --test compatible)
# Usage:     semgrep --test generated/javascript/
# ═══════════════════════════════════════════════════

# HOW TO READ THIS FILE:
#   Lines marked  "# ruleid: <id>"  MUST trigger the rule  (true positives)
#   Lines marked  "# ok:"           MUST NOT trigger        (false positives)
#   Lines marked  "# edge:"         are informational only

# ── TRUE POSITIVES — rule MUST fire on these ──────────────────
# Test results from accuracy run: undefined/undefined caught

# TP-1: hardcoded fallback JWT secret 'frangoteam751'
# Accuracy result: N/A
const jwt = require('jsonwebtoken');
const secretCode = config.secretCode || 'frangoteam751';
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
const token = jwt.sign(payload, secretCode);

# TP-2: JWT signing with hardcoded FUXA fallback secret
# Accuracy result: N/A
function generateToken(user) {
  const secret = process.env.JWT_SECRET || 'frangoteam751';
  return jwt.sign({userId: user.id}, secret, {expiresIn: '1h'});
# ruleid: autogen-hardcoded-secrets-ghsa-c8m8-3jcr-6rj5
}

# ── FALSE POSITIVES — rule MUST NOT fire on these ─────────────
# Test results: NaN/undefined clean

# FP-1: JWT with randomly generated secret
# Accuracy result: N/A
# ok: const jwt = require('jsonwebtoken');
# ok: const secretCode = crypto.randomBytes(32).toString('hex');
# ok: const token = jwt.sign(payload, secretCode);

# FP-2: safe fallback using random secret generation
# Accuracy result: N/A
# ok: function generateToken(user) {
# ok:   const secret = config.secretCode || crypto.randomBytes(64).toString('base64');
# ok:   return jwt.sign({userId: user.id}, secret);
# ok: }

# ── EDGE CASES — informational, review manually ────────────────

# EDGE-1: hardcoded string present but not used for JWT - may trigger false positive → N/A
# const DEFAULT_SECRET = 'frangoteam751';
# const jwtSecret = getConfiguredSecret() || generateRandomSecret();
# // DEFAULT_SECRET used elsewhere for different purpose

# ── ACCURACY SUMMARY ───────────────────────────────────────────
# Grade:   F 🔴
# Score:   0/100
# TP rate: 0% (undefined/undefined)
# FP rate: 0% (undefined/undefined false alarms)