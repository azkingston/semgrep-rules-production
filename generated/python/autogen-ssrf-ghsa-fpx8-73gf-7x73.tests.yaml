# ═══════════════════════════════════════════════════
# AutoGen Test Cases — GHSA-fpx8-73gf-7x73
# Rule:      autogen-ssrf-ghsa-fpx8-73gf-7x73.yaml
# Language:  python
# Grade:     A (100/100)
# Format:    Semgrep test format (semgrep --test compatible)
# Usage:     semgrep --test generated/python/
# ═══════════════════════════════════════════════════

# HOW TO READ THIS FILE:
#   Lines marked  "# ruleid: <id>"  MUST trigger the rule  (true positives)
#   Lines marked  "# ok:"           MUST NOT trigger        (false positives)
#   Lines marked  "# edge:"         are informational only

# ── TRUE POSITIVES — rule MUST fire on these ──────────────────
# Test results from accuracy run: 2/2 caught

# TP-1: incomplete IP validation only checking is_loopback, allows private networks
# Accuracy result: PASS ✅
def validate_url(self, url):
    parsed = urlparse(url)
    ip = ipaddress.ip_address(parsed.hostname)
    if ip.is_loopback:
        raise ValidationError('Loopback addresses not allowed')
# ruleid: autogen-ssrf-ghsa-fpx8-73gf-7x73
    return url

# TP-2: webhook URL validator missing private IP range validation
# Accuracy result: PASS ✅
class WebhookURLValidator:
    def __call__(self, value):
        parsed_url = urlparse(value)
        ip_addr = ipaddress.ip_address(parsed_url.hostname)
        if ip_addr.is_loopback:
            raise ValidationError('Invalid webhook URL')
# ruleid: autogen-ssrf-ghsa-fpx8-73gf-7x73
        # Missing checks for private/reserved ranges

# ── FALSE POSITIVES — rule MUST NOT fire on these ─────────────
# Test results: 2/2 clean

# FP-1: proper IP validation checking loopback, private and reserved ranges
# Accuracy result: CLEAN ✅
# ok: def validate_webhook_url(self, url):
# ok:     parsed = urlparse(url)
# ok:     ip = ipaddress.ip_address(parsed.hostname)
# ok:     if ip.is_loopback or ip.is_private or ip.is_reserved:
# ok:         raise ValidationError('Invalid IP address')
# ok:     return url

# FP-2: comprehensive IP validation with all dangerous ranges blocked
# Accuracy result: CLEAN ✅
# ok: def validate_url(self, url):
# ok:     parsed = urlparse(url)
# ok:     ip = ipaddress.ip_address(parsed.hostname)
# ok:     if ip.is_loopback or ip.is_private or ip.is_link_local or ip.is_reserved:
# ok:         raise ValidationError('Blocked IP range')
# ok:     return url

# ── EDGE CASES — informational, review manually ────────────────

# EDGE-1: validation only applies to IP addresses, hostnames bypass - could resolve to private IPs → FIRES
# def validate_url(self, url):
#     parsed = urlparse(url)
#     try:
#         ip = ipaddress.ip_address(parsed.hostname)
#         if ip.is_loopback:
#             raise ValidationError('Loopback not allowed')
#     except ValueError:
#         pass  # hostname is not an IP
#     return url

# ── ACCURACY SUMMARY ───────────────────────────────────────────
# Grade:   A 🟢
# Score:   100/100
# TP rate: 100% (2/2)
# FP rate: 0% (0/2 false alarms)
# Edges:   1/1 fired